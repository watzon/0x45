<section>
<h1>Paste69</h1>

<p>
    Paste69 is a fast and open source pastebin, file bin, and link shortener written in Go.
</p>

<p>
    <a href="https://github.com/watzon/0x45">Star the Project on GitHub ⭐</a>
</p>
</section>

<section>
<div class="info-box">
    <h3>📦 CLI Tool Available!</h3>
    <p>Install our CLI tool for easier interaction:</p>
    <div class="code-block">
        <code>go install github.com/watzon/0x45-cli/cmd/0x45@latest</code>
        <button class="action-btn" data-clipboard data-clipboard-content="go install github.com/watzon/0x45-cli/cmd/0x45@latest">
            <span>Copy</span>
        </button>
    </div>
</div>
</section>

<section>
<h2>Usage</h2>

<div class="command-group">
    <div class="labeled-code-block">
        <span class="command-label curl-label">CURL</span>
        <div class="code-block">
            <pre><code>curl -F 'file=@file.txt' {{baseUrl}}/p</code></pre>
            <button class="action-btn" data-clipboard data-clipboard-content="curl -F 'file=@file.txt' {{baseUrl}}/p">
                <span>Copy</span>
            </button>
        </div>
    </div>
</div>

<div class="command-group">
    <div class="labeled-code-block">
        <span class="command-label curl-label">CURL</span>
        <div class="code-block">
            <pre><code>curl -X POST --data-binary @file.txt {{baseUrl}}/p</code></pre>
            <button class="action-btn" data-clipboard data-clipboard-content="curl -X POST --data-binary @file.txt {{baseUrl}}/p">
                <span>Copy</span>
            </button>
        </div>
    </div>
</div>

<div class="command-group">
    <div class="labeled-code-block">
        <span class="command-label curl-label">CURL</span>
        <div class="code-block">
            <pre><code>curl -H "Authorization: Bearer YOUR_API_KEY" -F "url=https://example.com" {{baseUrl}}/u</code></pre>
            <button class="action-btn" data-clipboard data-clipboard-content='curl -H "Authorization: Bearer YOUR_API_KEY" -F "url=https://example.com" {{baseUrl}}/u'>
                <span>Copy</span>
            </button>
        </div>
    </div>
</div>

<p>
    For more examples and API docs, visit:<br />
    <a href="{{baseUrl}}/docs">{{baseUrl}}/docs</a>
</p>

<p>
    View detailed statistics at:<br />
    <a href="{{baseUrl}}/stats">{{baseUrl}}/stats</a>
</p>
</section>

<section>
<h2>File Retention</h2>

<p>
    Retention is the number of days a file will be available for download.
</p>

<ul>
    <li>Limits:</li>
    <ul>
        <li>No API Key: {{retention.noKey}}</li>
        <li>With API Key: {{retention.withKey}}</li>
        <li>Maximum Size: {{retention.maxSizeMiB}}</li>
    </ul>
</ul>

<div class="chart">
    <span data-chart data-chart-type="dot" data-chart-options='{
              "series": [
                  {"name": "No API Key", "palette": "blue", "data": {{{retention.noKeyHistory}}} },
                  {"name": "With API Key", "palette": "green", "data": {{{retention.withKeyHistory}}} }
              ],
              "height": 15,
              "barWidth": 30,
              "yAxisPadding": 12,
              "xAxisPadding": 1,
              "connectLines": true,
              "showScale": true,
              "showAxisLabels": true,
              "xAxis": {
                  "label": "File Size",
                  "unit": "MiB",
                  "points": 3,
                  "range": {
                      "min": 0,
                      "max": {{retention.maxSize}}
                  }
              },
              "yAxis": {
                  "label": "Days",
                  "points": 3,
                  "range": {
                      "min": {{retention.minAge}},
                      "max": {{retention.maxAge}}
                  }
              }
          }'>
    </span>
</div>
</section>