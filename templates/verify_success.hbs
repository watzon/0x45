<div class="verify-success">
    <h1>API Key Verified!</h1>
    
    <p>Your API key has been successfully verified. Here's your key:</p>
    
    <div class="code-block">
        <pre><code>{{apiKey}}</code></pre>
        <button class="action-btn" data-clipboard data-content="{{apiKey}}">
            <span>Copy</span>
        </button>
    </div>
    
    <div class="key-info">
        <h2>Using Your API Key</h2>
        <p>Include your API key in requests using the Authorization header:</p>
        
        <div class="code-block">
            <pre><code>Authorization: Bearer {{apiKey}}</code></pre>
            <button class="action-btn" data-clipboard data-content="Authorization: Bearer {{apiKey}}">
                <span>Copy</span>
            </button>
        </div>
        
        <p>For example:</p>
        <div class="code-block">
            <pre><code>curl -H "Authorization: Bearer {{apiKey}}" \
    -d '{"url":"https://example.com"}' \
    {{baseUrl}}/url</code></pre>
            <button class="action-btn" data-clipboard>
                <span>Copy</span>
            </button>
        </div>
        
        <p>See the <a href="/docs">API documentation</a> for more examples and details.</p>
        
        <div class="warning">
            <strong>Important:</strong> Keep your API key secure and do not share it with others.
            If your key is compromised, you'll need to request a new one.
        </div>
    </div>
</div> 